name: ci

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    steps:
      - name: Run Aspell
        continue-on-error: true
        uses: ktht/aspell@v2
        with:
          files: README.md
          args: --mode=markdown
